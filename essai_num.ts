// function generateRandomNumber(): number {
//     const min = 1000000000; // Minimum 10 digit number
//     const max = 9999999999; // Maximum 10 digit number
//     return Math.floor(Math.random() * (max - min + 1)) + min;
// }

// const randomNumber: number = generateRandomNumber();
// console.log(randomNumber);



/* recupaiement (amount:number, withdrawnBy:string) {
        
        this.history.push({
            amount,
            performedBy: withdrawnBy,
            trasactionType: TransactionType.WITHDRAWAL,
            date: new Date()
        })
        this.balance -= amount;

        console.log(`${amount} successfully withdrawn by ${withdrawnBy} solde actuel ${this.balance}!`); 
        
    } */
